{
  "name": "elysia-ceshi",
  "version": "1.0.50",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "docker build -t mcpdict_backend .",
    "deploy": "docker stop mcpdict_backend && docker rm mcpdict_backend && docker run -d -p 3000:3000 --name mcpdict_backend mcpdict_backend"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.1.1",
    "@elysiajs/swagger": "^1.1.6",
    "@vearvip/hanzi-utils": "1.0.8",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
